<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chat.auth.RefreshTokenMapper">

  <resultMap id="refreshTokenMap" type="com.example.chat.auth.RefreshToken">
    <id     property="id"        column="id"/>
    <result property="userId"    column="user_id"/>
    <result property="tokenHash" column="token_hash"/>
    <result property="familyId"  column="family_id"/>
    <result property="expiresAt" column="expires_at"/>
    <result property="revokedAt" column="revoked_at"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <sql id="columns">
    id, user_id, token_hash, family_id, expires_at, revoked_at, created_at
  </sql>

  <insert id="insert" parameterType="com.example.chat.auth.RefreshToken" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO refresh_tokens (user_id, token_hash, family_id, expires_at)
    VALUES (#{userId}, #{tokenHash}, #{familyId}, #{expiresAt})
  </insert>

  <select id="findByTokenHash" resultMap="refreshTokenMap">
    SELECT <include refid="columns"/>
    FROM refresh_tokens
    WHERE token_hash = #{tokenHash}
  </select>

  <update id="revokeById">
    UPDATE refresh_tokens SET revoked_at = CURRENT_TIMESTAMP(3)
    WHERE id = #{id} AND revoked_at IS NULL
  </update>

  <update id="revokeFamily">
    UPDATE refresh_tokens SET revoked_at = CURRENT_TIMESTAMP(3)
    WHERE family_id = #{familyId} AND revoked_at IS NULL
  </update>

  <update id="revokeAllByUserId">
    UPDATE refresh_tokens SET revoked_at = CURRENT_TIMESTAMP(3)
    WHERE user_id = #{userId} AND revoked_at IS NULL
  </update>

</mapper>
