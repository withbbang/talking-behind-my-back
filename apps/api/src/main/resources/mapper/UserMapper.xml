<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chat.user.UserMapper">

  <resultMap id="userMap" type="com.example.chat.user.User">
    <id     property="id"              column="id"/>
    <result property="nickname"        column="nickname"/>
    <result property="profileImageUrl" column="profile_image_url"/>
    <result property="role"            column="role"/>
    <result property="status"          column="status"/>
    <result property="lastLoginAt"     column="last_login_at"/>
    <result property="createdAt"       column="created_at"/>
    <result property="updatedAt"       column="updated_at"/>
    <result property="deletedAt"       column="deleted_at"/>
  </resultMap>

  <sql id="columns">
    id, nickname, profile_image_url, role, status, last_login_at, created_at, updated_at, deleted_at
  </sql>

  <insert id="insert" parameterType="com.example.chat.user.User" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO users (nickname, profile_image_url, role, status)
    VALUES (#{nickname}, #{profileImageUrl}, #{role}, #{status})
  </insert>

  <select id="findById" resultMap="userMap">
    SELECT <include refid="columns"/>
    FROM users
    WHERE id = #{id} AND deleted_at IS NULL
  </select>

  <update id="updateLastLoginAt">
    UPDATE users SET last_login_at = CURRENT_TIMESTAMP(3)
    WHERE id = #{id} AND deleted_at IS NULL
  </update>

  <update id="updateStatus">
    UPDATE users SET status = #{status}
    WHERE id = #{id} AND deleted_at IS NULL
  </update>

  <update id="updateNickname">
    UPDATE users SET nickname = #{nickname}
    WHERE id = #{id} AND deleted_at IS NULL
  </update>

</mapper>
