<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chat.persona.PersonaMapper">

  <resultMap id="personaMap" type="com.example.chat.persona.Persona">
    <id     property="id"           column="id"/>
    <result property="name"         column="name"/>
    <result property="systemPrompt" column="system_prompt"/>
    <result property="active"       column="is_active"/>
    <result property="createdAt"    column="created_at"/>
    <result property="updatedAt"    column="updated_at"/>
  </resultMap>

  <sql id="columns">
    id, name, system_prompt, is_active, created_at, updated_at
  </sql>

  <select id="findActive" resultMap="personaMap">
    SELECT <include refid="columns"/>
    FROM personas
    WHERE is_active = 1
    ORDER BY id DESC
    LIMIT 1
  </select>

  <select id="findById" resultMap="personaMap">
    SELECT <include refid="columns"/>
    FROM personas
    WHERE id = #{id}
  </select>

  <select id="findAll" resultMap="personaMap">
    SELECT <include refid="columns"/>
    FROM personas
    ORDER BY is_active DESC, id ASC
  </select>

  <insert id="insert" parameterType="com.example.chat.persona.Persona" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO personas (name, system_prompt, is_active)
    VALUES (#{name}, #{systemPrompt}, #{active})
  </insert>

  <update id="update" parameterType="com.example.chat.persona.Persona">
    UPDATE personas
    SET name = #{name}, system_prompt = #{systemPrompt}
    WHERE id = #{id}
  </update>

  <update id="deactivateAll">
    UPDATE personas SET is_active = 0 WHERE is_active = 1
  </update>

  <update id="activate">
    UPDATE personas SET is_active = 1 WHERE id = #{id}
  </update>

  <delete id="deleteById">
    DELETE FROM personas WHERE id = #{id} AND is_active = 0
  </delete>

</mapper>
